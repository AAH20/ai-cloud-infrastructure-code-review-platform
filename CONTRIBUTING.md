# Contributing

Contributions are welcome for provider adapters, deterministic policies, tests, documentation and deployment modules. Every adapter must preserve raw-source attribution, version information, tenant boundaries and explicit evidence classification. No adapter may silently mutate infrastructure.

Run `PYTHONPATH=src python -m unittest discover -s tests -v` before opening a pull request.

